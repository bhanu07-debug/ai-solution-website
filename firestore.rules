
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read access to all collections for viewing content.
    // Allow write access for creating new documents (e.g., feedback, inquiries).
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
